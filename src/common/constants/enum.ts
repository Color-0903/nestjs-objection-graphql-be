export enum USER_TYPE {
    ADMIN = 'administrator',
    CUSTOMER = 'customer',
  }
  
  export enum ASEET_TYPE {
    IMAGE = 'IMAGE',
    VIDEO = 'VIDEO',
    BINARY = 'BINARY',
    FOLDER = 'FOLDER',
  }
  
  export enum STATUS_RETURN {
    SUCCESS = 'success',
    FAILED = 'failed',
  }
    
  export enum DELETE_TYPE {
    SORT = 'sort',
    HARD = 'hard'
  }